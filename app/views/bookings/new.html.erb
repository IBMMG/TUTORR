<h1>Make a booking</h1>


<%= simple_form_for [@lesson, @booking] do |b| %>

  <%= b.input :name %>
  <%= b.input :date %>
  <%= b.submit class: "btn btn-primary newbutton" %>
<% end %>

<%= link_to 'Back to Lessons', lessons_path, class: "btn btn-primary newbutton"  %>


