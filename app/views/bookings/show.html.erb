


<div class="container col-6 ">
  <h1>YOUR BOOKING</h1>
  <div class="card-product">
    <% if @lesson.photo? %>
    <%= cl_image_tag @lesson.photo, width:160, height:160 %>
    <% else %>
    <%= image_tag image_url("code3.jpg"), width:160, height:160 %>
    <% end %>

    <div class="card-header">
      Booking:
    </div>
    <div class=" card-product-infos">

      <h2><%= @booking.lesson.name.upcase %> </h2>
      <p>Date: <%= @booking.date %> </p>
      <p>Price: <strong><%= @booking.lesson.price %> €  </strong> </p>
    </div>

    <div class="card-body  d-flex justify-content-end">
      <a href="/bookings" class="card-link btn btn-outline-success newbutton ">Back to Bookings</a>
    </div>

  </div>
</div>
